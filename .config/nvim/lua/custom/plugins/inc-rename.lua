return {
  -- Incremental rename: live-previews the rename across all occurrences
  -- in the buffer as you type, instead of renaming blind.
  'smjonas/inc-rename.nvim',
  cmd = 'IncRename',
  config = true,
}
