# Stow default ignores (must be re-listed here — .stow-local-ignore replaces, not extends, the defaults)
\.git
\.gitignore
\.gitmodules
CVS
\.svn
RCS
^/$

# Dotfiles repo tooling — not for stow
lefthook.yml
\.gitleaksignore
README\.md
install\.sh
scripts

# Claude dirs — kept as real dirs with individual file symlinks inside (Claude Code uses lstat)
\.claude/commands
\.claude/skills
\.claude/agents

# Apps that write back to their config — keep as real files, not symlinks
\.claude/settings\.json
\.claude/plugins/installed_plugins\.json
\.claude/plugins/blocklist\.json
\.config/karabiner/karabiner\.json
\.config/opencode/opencode\.json
